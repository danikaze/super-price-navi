import { BarcodeReaderFile } from '../components/barcode-reader-file';

export default () => (
  <>
    <div>
      <strong>Build Id:</strong> {BUILD_ID}
    </div>
    <BarcodeReaderFile />
  </>
);
